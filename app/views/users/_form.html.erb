<%= form_with(model: user, local: true) do |form| %>
  <div class="form-group">
    氏名 <%= form.text_field :name %>
  </div>

  <div class="form-group">
    年齢 <%= form.text_field :age %>
  </div>

  <%= form.submit "送信" %>
<% end %>
